# ================================
# АЮРВЕДА БОТ — Python залежності
# ================================

# Telegram Bot Framework (асинхронний, сучасний)
aiogram==3.13.1

# Асинхронний HTTP-клієнт та веб-сервер
# Використовуємо версію сумісну з aiogram 3.13.1 (який вимагає aiohttp<3.11)
aiohttp~=3.10.0

# Асинхронна робота з файлами
aiofiles==24.1.0

# Завантаження .env файлу
python-dotenv==1.0.1

# Робота з датами
python-dateutil==2.9.0

# Логування
loguru==0.7.2

# Для Render.com — production сервер
# (gunicorn не підтримує Python 3.14 на Windows — використовуємо aiohttp напряму)

# База даних (асинхронна SQLite)
aiosqlite==0.20.0
